import { StyleSheet } from 'react-native'

const is = StyleSheet.create({
    inputContainer: {
        height: 40,
        marginTop: 30
    },
    input: {
        color: '#FFF',
        fontSize: 18,
        height: 55,
        width: '100%',
        borderBottomWidth: 0.5,
        textAlign: 'center'
    }
})

export default is